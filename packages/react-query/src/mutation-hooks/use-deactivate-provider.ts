/**
 * This file was auto-generated by scripts/generate-mutation-hooks.ts
 * Do not make direct changes to the file.
 */

import { useSavvyCalClient } from "../provider";
import type { Client, MutationOptionsFor } from "../client";

interface Options
  extends MutationOptionsFor<"delete", "/v1/providers/{provider_id}"> {
  client?: Client;
}

export const useDeactivateProvider = (options?: Options) => {
  const { client: overrideClient, ...mutationOptions } = options ?? {};
  const client = useSavvyCalClient(overrideClient);

  return client.useMutation(
    "delete",
    "/v1/providers/{provider_id}",
    mutationOptions,
  );
};
